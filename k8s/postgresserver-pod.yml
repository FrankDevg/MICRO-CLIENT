apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    app: minegocioapp
    name: postgres-pod
spec:
  containers:
    - name: postgres
      image: postgres:latest
      ports:
        - containerPort: 5432
      env:
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=compose-258456